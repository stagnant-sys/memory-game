import React from "react";

export default function Header () {
  return (
    <header className="header">
      <h1 className="header_title">Fragments of Memories</h1>
      <h3 className="header_subtitle">A Final Fantasy memory game</h3>
      <div>Click on each card, but only once!</div>
    </header>
  )
}